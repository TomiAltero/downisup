import React from "react";
import Typography from "@mui/material/Typography";

export default function Inicio() {
  return (
    <Typography variant="body1" color="text.secondary">
      Panel de inicio (proximamente)
    </Typography>
  );
}
